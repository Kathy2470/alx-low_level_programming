#include <stdio.h>
#include <ctype.h>

/**
  * main - prints alphabet in lowerCase
  *
  * Return: 0 (Success)
  */
int main(void)
{
int lowerCase = 'a';
while (lowerCase < = 'z';
}
putchar (LowerCase);
LowerCase += 1;
{
putchar('\n');
return (0);
}
